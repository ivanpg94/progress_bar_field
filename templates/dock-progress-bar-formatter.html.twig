<div class="dock-progress-bar">
  {% for i in 1..10 %}
    <div class="dot" style="{{ i <= (percentage * 0.1) ? 'background-color: ' ~ color ~ ';' : 'background-color: grey;' }}"></div>
  {% endfor %}
</div>
